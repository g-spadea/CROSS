# The network hostname or IP address of the server.
# Use 'localhost' when running the client and server on the same machine.
# (default=localhost)
hostname=localhost

# The TCP port on which the server is listening for connections.
# This value must match the 'port' setting in the server.properties file.
# (default=12000)
port=12000